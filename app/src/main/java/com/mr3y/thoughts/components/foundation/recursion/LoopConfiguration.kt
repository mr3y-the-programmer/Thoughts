package com.mr3y.thoughts.components.foundation.recursion

/**
 * Holds some default opinionated values for configuring the loop
 */
object LoopConfiguration {
    var iterationsNum = -90

    var initialShapeScaleFactor = 1.0f

    var initialTickUpScaleFactor = 1.0f

    var initialTickDownScaleFactor = 1.0f

    const val DEFAULT_ITERATIONS_INTERVAL = 6
}
